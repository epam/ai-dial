---
title: "DIAL SDK reference"
type: reference
persona: app-dev
component: sdk
last_verified: 2026-05-29
owner: "@dial-docs-team"
---

# DIAL SDK reference

DIAL SDK is a Python framework for building Custom Apps and model adapters that plug into the DIAL platform. It wraps [FastAPI](https://fastapi.tiangolo.com/) and handles the Unified API contract — streaming, error formatting, auth header propagation, and telemetry — so you can focus on application logic.

## Installation

Install the base package:

```bash
pip install aidial-sdk
```

To include OpenTelemetry support for traces, metrics, and logs:

```bash
pip install "aidial-sdk[telemetry]"
```

**Python version:** 3.8.1 or later.

## Package overview

| Module | Description | Reference |
|--------|-------------|-----------|
| `aidial_sdk` | `DIALApp` entry point, `HTTPException` | [DIALApp](1.dial-app.md) |
| `aidial_sdk.chat_completion` | `ChatCompletion` ABC, `Request`, `Response`, `Choice`, `Stage` | [Chat completion](2.chat-completion/0.index.md) |
| `aidial_sdk.embeddings` | `Embeddings` ABC, request and response models | [Embeddings](3.embeddings.md) |
| `aidial_sdk.exceptions` | Error classes for Unified API error responses | [Exceptions](4.exceptions.md) |
| `aidial_sdk.telemetry` | `TelemetryConfig` and OpenTelemetry initialization | [Telemetry](5.telemetry.md) |
| `aidial_sdk.chat_completion.form` | `Button` and `form` decorator for interactive forms | [Choice](2.chat-completion/3.choice.md) |

## Quick example

A minimal echo app that repeats the user's last message:

```python
import uvicorn
from aidial_sdk import DIALApp
from aidial_sdk.chat_completion import ChatCompletion, Request, Response


class EchoApplication(ChatCompletion):
    async def chat_completion(
        self, request: Request, response: Response
    ) -> None:
        last_message = request.messages[-1]
        with response.create_single_choice() as choice:
            choice.append_content(last_message.text())


app = DIALApp()
app.add_chat_completion("echo", EchoApplication())

if __name__ == "__main__":
    uvicorn.run(app, port=5000)
```

## Source repository

The SDK source code and additional examples are available at [github.com/epam/ai-dial-sdk](https://github.com/epam/ai-dial-sdk).

## Next steps

- [Getting started with DIAL SDK](../../1.apps/5.custom-apps/1.getting-started-sdk.md) walks you through building, containerizing, and testing an app end to end.
- [Tutorial: custom adapter](../../3.adapters/1.tutorial-custom-adapter.md) shows how to build a model adapter using the same SDK classes.
- [Custom App examples](../../1.apps/5.custom-apps/7.examples.md) lists runnable sample applications.
