---
title: "Control cost"
type: how-to
persona: devops
component: core
last_verified: 2026-06-03
owner: "@dial-docs-team"
---

# Control cost

Cap and monitor what your DIAL deployment spends on model calls and infrastructure. This guide is the operational hub: it points to the settings that enforce limits, the pipeline that measures spend, and the levers that lower it. It assumes a working deployment with access to `config.json` and your observability stack.

For why DIAL enforces limits at all, read [Usage limits and cost control](../../2.understand-dial/4.security-and-governance/3.usage-limits-and-cost-control.md). This page is how to act on it.

## Prerequisites

- DIAL Core with edit access to `config.json` (roles and keys)
- The Analytics Realtime pipeline available, to measure spend

## Step 1: Enforce limits per role and per key

The primary cost guardrail is a limit on tokens, cost, and requests, set on roles and API keys in `config.json`. A user inherits the limits of their role; a per-request or application key can carry its own. Configure these in the dedicated guide:

- [Configure roles and rate limits](../5.auth-and-access-control/3.roles-and-rate-limits.md) — token, cost, and request limits per role and deployment

Apply limits to every role that can reach an expensive model. An unlimited default role is the most common source of runaway spend.

## Step 2: Measure spend with Analytics Realtime

You cannot control what you do not measure. The Analytics Realtime pipeline turns DIAL Core request logs into per-request token and cost metrics in InfluxDB, which you visualize in Grafana. Set it up with:

- [Analytics Realtime setup](../6.observability/5.analytics-realtime-setup.md) — capture token usage and cost by user, model, and time

Use the dashboards to find the models, roles, and applications that drive cost, then tighten their limits from Step 1.

## Step 3: Alert before the bill, not after

Add alerts on cost and token-rate metrics so a spike pages you instead of surprising you at month end:

- [Alerting](../6.observability/4.alerting.md) — rules for token-limit exhaustion and cost thresholds

## Step 4: Pull the cost levers

Beyond limits, several settings lower the cost of the same workload:

- **Route to the right model.** Use cheaper models for cheaper tasks and reserve premium models for where they pay off. Configure model `upstreams` and routing in [Models configuration](../4.configuration/1.core/2.config-json/1.models.md).
- **Cache repeated prompt prefixes.** Prompt caching cuts the cost of requests that share a large common prefix. See [Prompt caching](../../3.building-with-dial/8.advanced-topics/1.prompt-caching.md).
- **Right-size infrastructure.** Match the managed cache tier and service replica counts to real demand rather than peak guesses; pair with [Scale DIAL for load](./2.scaling.md) so capacity follows usage down as well as up.

## Result

Every role has a limit that caps its spend, Analytics Realtime shows where money goes, alerts fire before budgets are blown, and routing, caching, and right-sizing reduce the cost of the workload itself.

## Related tasks

- [Configure roles and rate limits](../5.auth-and-access-control/3.roles-and-rate-limits.md) — the enforcement mechanism
- [Analytics Realtime setup](../6.observability/5.analytics-realtime-setup.md) — the measurement pipeline
- [Usage limits and cost control](../../2.understand-dial/4.security-and-governance/3.usage-limits-and-cost-control.md) — the concepts behind both

## Next steps

- [Scale DIAL for load](./2.scaling.md) — scale down idle capacity to save cost
- [Metrics and monitoring](../6.observability/2.metrics-and-monitoring.md) — the dashboards cost decisions rely on
