---
version: '1'

services:
  echo:
    build: ../dial-sdk/examples/echo
  image-size:
    build: ../dial-sdk/examples/image_size
  render-text:
    build: ../dial-sdk/examples/render_text
  core:
    ports:
      - "8080:8080"
    image: epam/ai-dial-core:development
    environment:
      'JAVA_OPTS': '-Dgflog.config=/opt/core/gflog.xml'
      'proxy.config.files': '["/opt/core/config.json"]'
      'aidial.identityProviders.test.rolePath': 'roles'
      'aidial.identityProviders.test.disableJwtVerification': 'true'
      'aidial.encryption.salt': 'salt'
      'aidial.encryption.password': 'password'
    volumes:
      - ${DIAL_DIR:-.}/core:/opt/core:Z
      - ${DIAL_DIR:-.}/core-logs/:/opt/epam/aidial/log:Z
  notebooks:
    depends_on:
      echo:
        condition: service_started
      image-size:
        condition: service_started
      render-text:
        condition: service_started
      core:
        condition: service_healthy
    build: python-notebooks-runner
    environment:
      'DIAL_URL': 'http://core:8080'
    volumes:
      - ./examples:/opt/examples:Z