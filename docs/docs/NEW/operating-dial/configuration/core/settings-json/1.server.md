---
title: "Server & Client"
type: reference
persona: devops
component: core
last_verified: 2026-04-29
owner: "@dial-core-team"
---

# Server & Client

## Server

Server settings configure the DIAL Core HTTP listener.

### `server.port`

HTTP port the server listens on.

**Default:** `8080`

```json
{
  "server": {
    "port": 9090
  }
}
```

### `server.acceptBacklog`

TCP accept backlog queue size. Increase for high-connection-rate deployments.

**Default:** `4096`

### `server.idleTimeout`

Maximum idle connection time in milliseconds. Applies to HTTP/1.1 keep-alive and HTTP/2 connections.

**Default:** `300000` (5 minutes)

### `server.compressionSupported`

Enable HTTP response compression.

**Default:** `true`

> **Note:** Additional Vert.x `HttpServerOptions` can be passed under `server.*`.

---

## Client

Client settings configure outbound HTTP connections to adapters and upstreams.

### `client.idleTimeout`

Idle timeout for outbound connections in milliseconds.

**Default:** `300000` (5 minutes)

### `client.connectTimeout`

Connection timeout for outbound connections in milliseconds.

**Default:** `10000` (10 seconds)

### `client.keepAlive`

Enable HTTP keep-alive for connection reuse.

**Default:** `true`

### `client.maxPoolSize`

Maximum connection pool size for outbound requests.

**Default:** `4096`

> **Note:** Additional Vert.x `HttpClientOptions` can be passed under `client.*`. WebSocket client settings are available under `webSocketClient.*`.

## Next steps

- [Storage](storage) — blob storage configuration
- [settings.json overview](index) — file location and precedence
