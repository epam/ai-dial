---
version: '3'

include:
  - ${COMMON_DIR:-..}/common.yml

services:
  assistant:
    image: epam/ai-dial-assistant:0.5.0
    environment:
      OPENAI_API_BASE: "http://core:8080"
      WEB_CONCURRENCY: "3"

  addon:
    build: addon